{
  "id": "openclaw-must-win",
  "name": "OpenClaw Must Win",
  "description": "Attributes Git commits created by OpenClaw processes to the active model and runtime.",
  "version": "0.1.0",
  "activation": {
    "onStartup": true,
    "onCapabilities": ["hook"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["required", "best-effort"],
        "default": "required",
        "description": "Whether ambiguous OpenClaw commit attribution blocks the commit."
      }
    }
  }
}
